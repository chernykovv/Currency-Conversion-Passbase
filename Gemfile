source 'https://rubygems.org'
gem 'sinatra', :github => 'sinatra/sinatra'

gem 'require_all'
gem 'puma', '~>5.5.2'
gem 'activerecord', '6.0.4'
gem 'sinatra-activerecord'
gem 'rake'

group :development do
  # use to enable auto reload
  gem 'sinatra-contrib'
  gem 'pg'
end

group :test do
  gem "rspec"
  gem "rack-test"
  gem 'database_cleaner-active_record'
end
